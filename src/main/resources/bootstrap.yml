spring:
  application:
    name: ninja-cart-service
  cloud:
    config:
      uri: http://192.168.1.7:60090

server:
  port: 60020

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}